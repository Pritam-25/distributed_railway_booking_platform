/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Railway Booking Platform API
 *
 *   # Railway Booking Platform API
 *
 *   Welcome to the **Railway Booking Platform API**. This document is the
 *   unified public API contract for the IRCTC Railway Booking Platform,
 *   combining every public endpoint exposed through the API Gateway.
 *
 *   ## Services
 *
 *   - **User Service** — Authentication, identity, and profile management.
 *   - **Admin Service** — Administrator authentication and administrative operations.
 *   - **Search Service** — Train and station search.
 *   - **Inventory Service** - Inventory Schedule Management.
 *   - **Booking Service** — Seat reservation and booking lifecycle. *(future)*
 *   - **Payment Service** — Payment processing. *(future)*
 *   - **Notification Service** — User-facing notifications. *(internal)*
 *
 * OpenAPI spec version: 1.0.0
 */
import { useMutation, useQuery } from "@tanstack/react-query"
import type {
  DataTag,
  DefinedInitialDataOptions,
  DefinedUseQueryResult,
  MutationFunction,
  QueryClient,
  QueryFunction,
  QueryKey,
  UndefinedInitialDataOptions,
  UseMutationOptions,
  UseMutationResult,
  UseQueryOptions,
  UseQueryResult,
} from "@tanstack/react-query"

import type {
  BookingsCancelBooking200,
  BookingsCreateBooking201,
  BookingsGetBooking200,
  CreateBookingRequest,
  ErrorResponse,
} from "../../model"

import { customInstance } from "../../../lib/api-client"

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1]

const withQueryKey = <T extends object, K>(
  query: T,
  queryKey: K
): T & { queryKey: K } => {
  const result = { queryKey } as T & { queryKey: K }
  for (const key of Object.keys(query)) {
    // The explicit queryKey always wins, matching the previous
    // `{ ...query, queryKey }` spread where it was set last.
    if (key === "queryKey") continue
    Object.defineProperty(result, key, {
      enumerable: true,
      configurable: true,
      get: () => (query as Record<string, unknown>)[key],
    })
  }
  return result
}

/**
 * Initiates a new booking in the `PENDING` state for the authenticated user. Idempotent on the `idempotencyKey` request field — a retry with the same key returns the original response without re-running the saga.
 *
 * **Request Body Fields:**
 * - `idempotencyKey` - Client-generated UUID that deduplicates retries.
 * - `scheduleId` - UUID of the `ScheduleInventory` row to book against.
 * - `fromStationId` / `toStationId` - Station UUIDs or station codes (e.g. BCK, HWR) of the segment endpoints (must differ).
 * - `seatIds` - 1 to 6 seat UUIDs from the seat-map.
 * - `passengers` - 1 to 6 passenger records, one per seat.
 *
 * **Response:**
 * Returns the new booking's id, PNR, and current status in the success envelope's `data` field.
 *
 * **Outcomes:**
 * - 201 Created - Booking was created in the `PENDING` state.
 * - 400 Bad Request - The request body failed schema validation.
 * - 401 Unauthorized - The JWT is missing or invalid.
 * - 404 Not Found - The schedule or one of the seats does not exist.
 * - 409 Conflict - The seats are already held by another booking or the saga cannot proceed.
 * - 429 Too Many Requests - Booking attempts were rate-limited.
 * - 500 Internal Server Error - The service could not complete the booking.
 *
 * **Notes:**
 * - The PNR is generated server-side (10-character alphanumeric) and returned only once in the 201 response. Subsequent GETs on the booking id return the same PNR.
 * - Idempotency keys are stored for the lifetime of the booking; reuse them only for genuine retries, not for distinct bookings.
 *
 * @summary Create Booking
 */
export const bookingsCreateBooking = (
  createBookingRequest: CreateBookingRequest,
  options?: SecondParameter<typeof customInstance>,
  signal?: AbortSignal
) => {
  return customInstance<BookingsCreateBooking201>(
    {
      url: `/api/v1/bookings`,
      method: "POST",
      headers: { "Content-Type": "application/json" },
      data: createBookingRequest,
      signal,
    },
    options
  )
}

export const getBookingsCreateBookingQueryKey = (
  createBookingRequest?: CreateBookingRequest
) => {
  return ["POST", `/api/v1/bookings`, createBookingRequest] as const
}

export const getBookingsCreateBookingQueryOptions = <
  TData = Awaited<ReturnType<typeof bookingsCreateBooking>>,
  TError = ErrorResponse,
>(
  createBookingRequest: CreateBookingRequest,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCreateBooking>>,
        TError,
        TData
      >
    >
    request?: SecondParameter<typeof customInstance>
  }
) => {
  const { query: queryOptions, request: requestOptions } = options ?? {}

  const queryKey =
    queryOptions?.queryKey ??
    getBookingsCreateBookingQueryKey(createBookingRequest)

  const queryFn: QueryFunction<
    Awaited<ReturnType<typeof bookingsCreateBooking>>
  > = ({ signal }) =>
    bookingsCreateBooking(createBookingRequest, requestOptions, signal)

  return { queryKey, queryFn, ...queryOptions } as UseQueryOptions<
    Awaited<ReturnType<typeof bookingsCreateBooking>>,
    TError,
    TData
  > & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type BookingsCreateBookingQueryResult = NonNullable<
  Awaited<ReturnType<typeof bookingsCreateBooking>>
>
export type BookingsCreateBookingQueryError = ErrorResponse

export function useBookingsCreateBooking<
  TData = Awaited<ReturnType<typeof bookingsCreateBooking>>,
  TError = ErrorResponse,
>(
  createBookingRequest: CreateBookingRequest,
  options: {
    query: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCreateBooking>>,
        TError,
        TData
      >
    > &
      Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof bookingsCreateBooking>>,
          TError,
          Awaited<ReturnType<typeof bookingsCreateBooking>>
        >,
        "initialData"
      >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): DefinedUseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
}
export function useBookingsCreateBooking<
  TData = Awaited<ReturnType<typeof bookingsCreateBooking>>,
  TError = ErrorResponse,
>(
  createBookingRequest: CreateBookingRequest,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCreateBooking>>,
        TError,
        TData
      >
    > &
      Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof bookingsCreateBooking>>,
          TError,
          Awaited<ReturnType<typeof bookingsCreateBooking>>
        >,
        "initialData"
      >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
}
export function useBookingsCreateBooking<
  TData = Awaited<ReturnType<typeof bookingsCreateBooking>>,
  TError = ErrorResponse,
>(
  createBookingRequest: CreateBookingRequest,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCreateBooking>>,
        TError,
        TData
      >
    >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
}
/**
 * @summary Create Booking
 */

/**
 *
 */
export function useBookingsCreateBooking<
  TData = Awaited<ReturnType<typeof bookingsCreateBooking>>,
  TError = ErrorResponse,
>(
  createBookingRequest: CreateBookingRequest,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCreateBooking>>,
        TError,
        TData
      >
    >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
} {
  const queryOptions = getBookingsCreateBookingQueryOptions(
    createBookingRequest,
    options
  )

  const query = useQuery(queryOptions, queryClient) as UseQueryResult<
    TData,
    TError
  > & { queryKey: DataTag<QueryKey, TData, TError> }

  return withQueryKey(query, queryOptions.queryKey)
}

/**
 * Returns the booking with the given id, scoped to the authenticated user. Returns 404 if the booking does not exist or belongs to another user (ownership is enforced before the booking is loaded for non-admins).
 *
 * **Request Body Fields:**
 * - `bookingId` (path) - UUID of the booking to fetch.
 *
 * **Response:**
 * Returns the full booking record (status, version, segments, seat/passenger rows) in the success envelope's `data` field.
 *
 * **Outcomes:**
 * - 200 OK - Booking was found and is owned by the authenticated user.
 * - 401 Unauthorized - The JWT is missing or invalid.
 * - 404 Not Found - The booking does not exist or belongs to another user.
 * - 429 Too Many Requests - Polling exceeded the default rate limit.
 * - 500 Internal Server Error - The service could not read the booking.
 *
 * **Notes:**
 * - The status field reflects the most recent CAS transition. Subscribe to the booking-events SSE stream (Phase 2) for live updates instead of polling.
 *
 * @summary Get Booking
 */
export const bookingsGetBooking = (
  bookingId: string,
  options?: SecondParameter<typeof customInstance>,
  signal?: AbortSignal
) => {
  return customInstance<BookingsGetBooking200>(
    { url: `/api/v1/bookings/${bookingId}`, method: "GET", signal },
    options
  )
}

export const getBookingsGetBookingMutationOptions = <
  TError = ErrorResponse,
  TContext = unknown,
>(options?: {
  mutation?: UseMutationOptions<
    Awaited<ReturnType<typeof bookingsGetBooking>>,
    TError,
    { bookingId: string },
    TContext
  >
  request?: SecondParameter<typeof customInstance>
}): UseMutationOptions<
  Awaited<ReturnType<typeof bookingsGetBooking>>,
  TError,
  { bookingId: string },
  TContext
> => {
  const mutationKey = ["bookingsGetBooking"]
  const { mutation: mutationOptions, request: requestOptions } = options
    ? options.mutation &&
      "mutationKey" in options.mutation &&
      options.mutation.mutationKey
      ? options
      : { ...options, mutation: { ...options.mutation, mutationKey } }
    : { mutation: { mutationKey }, request: undefined }

  const mutationFn: MutationFunction<
    Awaited<ReturnType<typeof bookingsGetBooking>>,
    { bookingId: string }
  > = (props) => {
    const { bookingId } = props ?? {}

    return bookingsGetBooking(bookingId, requestOptions)
  }

  return { mutationFn, ...mutationOptions }
}

export type BookingsGetBookingMutationResult = NonNullable<
  Awaited<ReturnType<typeof bookingsGetBooking>>
>

export type BookingsGetBookingMutationError = ErrorResponse

/**
 * @summary Get Booking
 */
export const useBookingsGetBooking = <
  TError = ErrorResponse,
  TContext = unknown,
>(
  options?: {
    mutation?: UseMutationOptions<
      Awaited<ReturnType<typeof bookingsGetBooking>>,
      TError,
      { bookingId: string },
      TContext
    >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseMutationResult<
  Awaited<ReturnType<typeof bookingsGetBooking>>,
  TError,
  { bookingId: string },
  TContext
> => {
  return useMutation(getBookingsGetBookingMutationOptions(options), queryClient)
}
/**
 * Cancels a booking owned by the authenticated user. Drives the row through `CANCELLING` → `CANCELLED` and emits one `BookingStatusChangedV1` event per transition.
 *
 * **Request Body Fields:**
 * - `bookingId` (path) - UUID of the booking to cancel.
 *
 * **Response:**
 * Returns the cancelled booking record (status `CANCELLED`) in the success envelope's `data` field.
 *
 * **Outcomes:**
 * - 200 OK - The booking was successfully cancelled.
 * - 401 Unauthorized - The JWT is missing or invalid.
 * - 404 Not Found - The booking does not exist or belongs to another user.
 * - 409 Conflict - The booking is already in a terminal state and cannot be cancelled.
 * - 429 Too Many Requests - Cancellation attempts were rate-limited.
 * - 500 Internal Server Error - The service could not complete cancellation.
 *
 * **Notes:**
 * - Cancellations on already-confirmed bookings will eventually trigger a refund via payment-service. The booking row flips to `CANCELLED` synchronously, but the refund is asynchronous.
 *
 * @summary Cancel Booking
 */
export const bookingsCancelBooking = (
  bookingId: string,
  options?: SecondParameter<typeof customInstance>,
  signal?: AbortSignal
) => {
  return customInstance<BookingsCancelBooking200>(
    { url: `/api/v1/bookings/${bookingId}/cancel`, method: "POST", signal },
    options
  )
}

export const getBookingsCancelBookingQueryKey = (bookingId: string) => {
  return ["POST", `/api/v1/bookings/${bookingId}/cancel`] as const
}

export const getBookingsCancelBookingQueryOptions = <
  TData = Awaited<ReturnType<typeof bookingsCancelBooking>>,
  TError = ErrorResponse,
>(
  bookingId: string,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCancelBooking>>,
        TError,
        TData
      >
    >
    request?: SecondParameter<typeof customInstance>
  }
) => {
  const { query: queryOptions, request: requestOptions } = options ?? {}

  const queryKey =
    queryOptions?.queryKey ?? getBookingsCancelBookingQueryKey(bookingId)

  const queryFn: QueryFunction<
    Awaited<ReturnType<typeof bookingsCancelBooking>>
  > = ({ signal }) => bookingsCancelBooking(bookingId, requestOptions, signal)

  return {
    queryKey,
    queryFn,
    enabled: bookingId !== null && bookingId !== undefined,
    ...queryOptions,
  } as UseQueryOptions<
    Awaited<ReturnType<typeof bookingsCancelBooking>>,
    TError,
    TData
  > & { queryKey: DataTag<QueryKey, TData, TError> }
}

export type BookingsCancelBookingQueryResult = NonNullable<
  Awaited<ReturnType<typeof bookingsCancelBooking>>
>
export type BookingsCancelBookingQueryError = ErrorResponse

export function useBookingsCancelBooking<
  TData = Awaited<ReturnType<typeof bookingsCancelBooking>>,
  TError = ErrorResponse,
>(
  bookingId: string,
  options: {
    query: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCancelBooking>>,
        TError,
        TData
      >
    > &
      Pick<
        DefinedInitialDataOptions<
          Awaited<ReturnType<typeof bookingsCancelBooking>>,
          TError,
          Awaited<ReturnType<typeof bookingsCancelBooking>>
        >,
        "initialData"
      >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): DefinedUseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
}
export function useBookingsCancelBooking<
  TData = Awaited<ReturnType<typeof bookingsCancelBooking>>,
  TError = ErrorResponse,
>(
  bookingId: string,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCancelBooking>>,
        TError,
        TData
      >
    > &
      Pick<
        UndefinedInitialDataOptions<
          Awaited<ReturnType<typeof bookingsCancelBooking>>,
          TError,
          Awaited<ReturnType<typeof bookingsCancelBooking>>
        >,
        "initialData"
      >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
}
export function useBookingsCancelBooking<
  TData = Awaited<ReturnType<typeof bookingsCancelBooking>>,
  TError = ErrorResponse,
>(
  bookingId: string,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCancelBooking>>,
        TError,
        TData
      >
    >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
}
/**
 * @summary Cancel Booking
 */

/**
 *
 */
export function useBookingsCancelBooking<
  TData = Awaited<ReturnType<typeof bookingsCancelBooking>>,
  TError = ErrorResponse,
>(
  bookingId: string,
  options?: {
    query?: Partial<
      UseQueryOptions<
        Awaited<ReturnType<typeof bookingsCancelBooking>>,
        TError,
        TData
      >
    >
    request?: SecondParameter<typeof customInstance>
  },
  queryClient?: QueryClient
): UseQueryResult<TData, TError> & {
  queryKey: DataTag<QueryKey, TData, TError>
} {
  const queryOptions = getBookingsCancelBookingQueryOptions(bookingId, options)

  const query = useQuery(queryOptions, queryClient) as UseQueryResult<
    TData,
    TError
  > & { queryKey: DataTag<QueryKey, TData, TError> }

  return withQueryKey(query, queryOptions.queryKey)
}
